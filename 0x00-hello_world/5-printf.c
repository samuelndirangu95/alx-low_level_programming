#include <stdio.h>
/**
 * main-prints  phrase with proper grammar, but the outcome is a piece of art,
 * Return: 0 to show function is successful, nonzero ig otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
